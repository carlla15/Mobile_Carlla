<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.minhabiblioteca.fragments.HomeFragment"
        android:label="Início"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_homeFragment_to_livrosFragment"
            app:destination="@id/livrosFragment" />
    </fragment>

    <fragment
        android:id="@+id/livrosFragment"
        android:name="com.example.minhabiblioteca.fragments.LivrosFragment"
        android:label="Livros"
        tools:layout="@layout/fragment_livros">

        <argument
            android:name="livroSelecionado"
            app:argType="string"
            android:defaultValue="Nenhum" />
    </fragment>

    <fragment
        android:id="@+id/perfilFragment"
        android:name="com.example.minhabiblioteca.fragments.PerfilFragment"
        android:label="Perfil"
        tools:layout="@layout/fragment_perfil" />

</navigation>